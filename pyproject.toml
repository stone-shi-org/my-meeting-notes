[tool.pytest.ini_options]
junit_family = "xunit2"
junit_logging = "system-out"
addopts = "--strict-markers"
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "integration: hits a real external binary or service",
]
filterwarnings = [
    "ignore::DeprecationWarning:websockets.*",
]
