fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.28.0
pyyaml>=6.0
mcp>=1.0.0
# Fernet, for encrypting integration credentials at rest. Already arrives as an
# mcp transitive dep, but pinned here because we now import it directly.
cryptography>=42.0.0
# SSE client for consuming the LLM proxy's streamed chat completions. Already
# arrives as an mcp transitive dep (its SSE transport), but pinned here because
# app/services/llm.py now imports it directly for the chat streaming endpoint.
httpx-sse>=0.4.0
# iCalendar parsing and recurrence expansion for CalDAV. icalendar alone does not
# expand RRULEs -- upstream calls it out of scope -- and hand-rolling EXDATE,
# RECURRENCE-ID overrides and DST-correct expansion is where the bugs live.
# Deliberately NOT the `caldav` package: it is built on requests, which respx
# cannot intercept, so it would take the test suite offline-unsafe.
icalendar>=6.0
recurring-ical-events>=3.0
# Async websocket client for live captions' persistent /v1/realtime session
# (app/routers/live_caption.py) -- already arrives transitively (fastapi/
# uvicorn pull it in), but pinned here because that module now imports it
# directly to hold one long-lived connection per channel for the life of a
# recording, not just the one-shot request/response respx already covers.
websockets>=13.0
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.10.0
respx>=0.21.0
# gRPC client for live-stt streaming ASR service (app/routers/live_caption.py)
grpcio>=1.60.0
protobuf>=4.25.0

